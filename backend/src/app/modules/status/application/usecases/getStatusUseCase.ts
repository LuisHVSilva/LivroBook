/*
import {IStatusRepository} from "../../ports/status/IStatusRepository";
import {Status} from "../../../domain/entities/status";
import {StateEnum} from "../../../domain/enums/enumState";


export class GetStatusUseCase {
    constructor(private statusRepository: IStatusRepository) {
    }

    async execute(statusId: StateEnum): Promise<Status | null> {
        return await this.statusRepository.findById(statusId);
    }
}
*/
